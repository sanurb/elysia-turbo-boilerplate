# Secret for Better Auth (must be a strong random string)
BETTER_AUTH_SECRET=zuleQm11C5ZWcpmSx9eCfR1SMUr2Cnn8

# Base URL of your app (used for callbacks, etc.)
BETTER_AUTH_URL=http://localhost:3000

# PostgreSQL connection string for local docker-compose
DATABASE_URL=postgres://betterauth:betterauthpassword@localhost:5432/betterauth

# SMTP configuration for email (required for email verification, password reset, etc.)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
SMTP_FROM=your@email.com
